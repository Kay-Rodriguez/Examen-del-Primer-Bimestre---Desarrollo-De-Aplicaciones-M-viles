import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'movilesKarla',
  appName: 'movistarexamen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
