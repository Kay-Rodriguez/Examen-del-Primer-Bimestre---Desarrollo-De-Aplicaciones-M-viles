import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.movileskarla.app',
  appName: 'movistarexamen',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
